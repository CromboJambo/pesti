// Stub tcgen05 GEMM kernel for CPU-only builds
// This is a minimal placeholder that compiles but does nothing useful
.version 8.7
.target sm_100
.address_size 64

// tcgen05 128x128x16 tile GEMM kernel (stub)
.visible .entry gemm_tcgen05_kernel(
    // Parameters: alpha, A, B, beta, C, m, n, k
    .param .u32 alpha,
    .param .param A,
    .param .param B,
    .param .u32 beta,
    .param .param C,
    .param .u32 m,
    .param .u32 n,
    .param .u32 k
)
{
    // Stub: just return immediately
    ret;
}
