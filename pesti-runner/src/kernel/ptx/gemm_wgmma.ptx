// Stub WGMMA GEMM kernel for CPU-only builds
// This is a minimal placeholder that compiles but does nothing useful
.version 7.0
.target sm_120
.address_size 64

// WGMMA 64x64x64 tile GEMM kernel (stub)
.visible .entry gemm_wgmma_kernel(
    // Parameters: alpha, A, B, beta, C, m, n, k
    .param .u32 alpha,
    .param .param A,
    .param .param B,
    .param .u32 beta,
    .param .param C,
    .param .u32 m,
    .param .u32 n,
    .param .u32 k
)
{
    // Stub: just return immediately
    ret;
}
