# Minimal Clippy Configuration for PESTI
# Only uses fields that work with clippy 0.1.97

# Maximum cognitive complexity (works in newer versions)
# Note: This field may not be recognized by all clippy versions
