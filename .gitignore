# Build artifacts (top-level and nested)
target/
**/target/
Cargo.lock
*.o
*.rlib
guard.db
knowledge.db
cobertura.xml

# Generated files
tarpaulin-report.html
tarpaulin-report.json

# Python / tool caches
__pycache__/
*.pyc
unsloth_compiled_cache/

# Model weights / large binary blobs
# models/  # Removed - merged into conformance-corpus/
models
projects/
state-docs/
conformance-corpus/*.gguf
# Compiled conformance oracles (rebuild with `cc -O2 -o oracle oracle.c`)
conformance-corpus/oracle
conformance-corpus/oracle_dbg
test_models/*.gguf
pesti-conformance/test-corpus/tiny-model.gguf
lms-models/
test_real_file_bin
pesti-gguf/src/parser.rs.bak
pesti-gguf/src/parser.rs.fixed
pesti-gguf/src/string_case.tmp

# HF reference tokenizers (re-downloadable from HF Hub, or re-derived from
# the GGUF via pesti-runner --example dump_tokenizer; the runtime builds
# tokenizers from GGUF-embedded tokenizer.ggml.* arrays instead)
assets/tokenizers/

# OS/editor junk
.DS_Store
*.swp
*.swo
*~

# Historical archive (kept locally, not committed)
historical/

# Orphaned / scratch code (not part of any workspace crate; kept locally)
scratch/

# Temporary diagnostic examples (move to git history or delete when no longer needed)
# Keep only: coherence_check.rs, probe_input_dep.rs, probe_layer_diff.rs
# Delete: debug_*.rs (legacy), probe_*.rs (replaced by Week 15 probes), hermes-*.rs (Hermes-specific)
